# cutlog 画面別 要件定義（web 版の棚卸し）

`web/index.html` と `web/app.js` を読み、**振る舞い**を画面ごとに書き出したもの。
Flutter 版はこれを満たすことを目標にする。行番号は `web/app.js`。

凡例: ✅ = Flutter 実装済み / ⛔ = 未実装 / △ = 一部のみ

---

## 0. 全画面に効く土台

| 事項 | 仕様 | 状態 |
| --- | --- | --- |
| API | `/api` 配下。401 なら state をクリアしてログイン画面へ（L35-45） | ✅ |
| トースト | 同時に1本だけ。既定 2600ms、失敗系は 4000〜6000ms（L47-53） | ✅ |
| 未処理の失敗 | `うまくいきませんでした: {訳}` を 5000ms（L2983-2987） | ⛔ |
| モーダル | 背景タップ・×・Esc・**下へ 110px 超スワイプ**で閉じる（L536-620） | ✅（標準の札に委譲） |
| 画面遷移 | logs:0 / log:1 / logset:2 の深さ。奥は下から、戻りは上から（L102-123） | ⛔ |
| 時刻 | `fmtTime` = `takenAt` から `tzOffset` 分を引いた `HH:MM`（L55-58） | ✅ |

---

## 1. ログ一覧（#logsScreen）

| # | 要件 | 状態 |
| --- | --- | --- |
| 1.1 | 行は「サムネ48px／名前／`Nカット · M人 · オーナー名 · 最後 MM.DD`／>」 | ✅ |
| 1.2 | 非公開ログは錠アイコン＋`Nカット · 非公開`（人数もオーナーも出さない） | ✅ |
| 1.3 | サムネは読み終わってからふわっと出す（L2600-2613） | △ |
| 1.4 | 行タップ → そのログを開く（`state.selected` を捨て、日画面を閉じてから） | ✅ |
| 1.5 | ＋ → ログを追加の札（新しく作る／招待コードで参加する） | ✅ |
| 1.6 | **作成後はそのまま撮影画面を開く**（L3124） | ⛔ |
| 1.7 | 参加成功で `ログに参加しました` → そのログを開く | ⛔ |
| 1.8 | 入力欄で Enter を押すと該当ボタンが発火（L3108-3113） | ⛔ |
| 1.9 | 虫めがね → ログを検索の札。**クライアント側で名前とオーナー名に部分一致** | △（名前のみ） |
| 1.10 | **絞り込み帯**「タイトル「X」で絞りこみ中」＋`解除`（L2590-2594） | ⛔ |
| 1.11 | 空の文言 2種（絞り込み有無で出し分け、L2619-2623） | ⛔ |
| 1.12 | ログが 0 件なら `マイログ` を自動生成（L245-257） | ⛔ |

## 2. ログ＝カレンダー（#app）

| # | 要件 | 状態 |
| --- | --- | --- |
| 2.1 | パンくず `ログ › {ログ名}`、右に設定ボタン | ✅ |
| 2.2 | **`#logMeta`**：非公開なら `非公開`、ほかは `N人`（L263） | ⛔ |
| 2.3 | **ツールバー**：メモ検索（350ms の遅延）／投稿者の絞り込み／タグの絞り込み（候補0なら隠す） | ⛔ |
| 2.4 | 月を縦に積む。範囲は「最古のカットの月」と「11か月前」の古い方から、今月または最新カットの月まで | ✅ |
| 2.5 | 月見出し `YYYY.MM` ＋ その月の `Nカット`（0 件なら空） | ✅ |
| 2.6 | 日は 2 桁ゼロ埋め。カットのある日だけ押せる（0件は disabled） | ✅ |
| 2.7 | 点は件数ぶん、**最大 6 個** | ✅ |
| 2.8 | 今日は下線 | ✅ |
| 2.9 | 開くたび最新の月へ即スクロール（アニメさせない） | ✅ |
| 2.10 | 絞り込みはサーバ側（`?q=&author=&tag=`）。カレンダーの点も連動して変わる | ⛔ |

## 3. このログの設定（#logSetScreen）

| # | 要件 | 状態 |
| --- | --- | --- |
| 3.1 | パンくず `ログ › {ログ名} › 設定` | ✅ |
| 3.2 | タイトル（80字）・1クリップの長さ（1〜30 にクランプ）・保存 | ✅ |
| 3.3 | **`デフォルトでこのログに保存する`**（`PATCH /me {defaultLogId}`。外すと privateLogId へ戻す） | ⛔ |
| 3.4 | **非オーナーは 名前・秒数・保存・再生成 が無効**（`state.role`） | ⛔ |
| 3.5 | 招待コードのコピー（失敗しても成功トースト）／再生成（確認あり） | ✅ |
| 3.6 | **非公開ログでは招待セクションごと隠す** | ⛔ |
| 3.7 | 参加者一覧（オーナー/メンバー表示）。**オーナーのみ、自分以外に `除外`**（確認あり） | △（一覧のみ） |
| 3.8 | ゴミ箱：`{日付} {時刻}　{メモ}` と `戻す`。空なら `消したカットはありません。` | ✅ |
| 3.9 | **共有リンクの一覧**：`{タイトル}` `{N}カット ・パスワードあり ・停止中`、`コピー` `停止`（確認あり） | ⛔ |

## 4. カット一覧（#allScreen）

| # | 要件 | 状態 |
| --- | --- | --- |
| 4.1 | `GET /cuts?q=&author=&limit=300`。日付ごとの見出し `YYYY.MM.DD` ＋ `NN cuts` | ✅ |
| 4.2 | セルは時刻＋ログ名のキャプション付き。使い回して再読み込みを避ける | △ |
| 4.3 | **長押し 450ms で選択モードに入り、押したセルが選ばれる**（振動 12ms） | ⛔ |
| 4.4 | 選択中のタップはトグル。**最後の1件を外すと選択モードを抜ける** | ⛔ |
| 4.5 | **操作バー**：`N件` `すべて選択` `書き出し` `移動` `削除` `やめる` | ⛔ |
| 4.6 | 削除は確認 →1件ずつ `DELETE`。失敗は個別トーストで続行 → `N件を削除しました` | ⛔ |
| 4.7 | ＋ → 端末の動画を複数取り込み（`file.lastModified` を撮影時刻にする） | ⛔ |
| 4.8 | 虫めがね → 検索の札（メモ／投稿者）。**絞り込み帯**「メモ「X」　{名前} のぶん　で絞りこみ中」 | △（札のみ） |
| 4.9 | 空の文言 2種 | ⛔ |

## 5. カットの詳細（#cutDialog）

| # | 要件 | 状態 |
| --- | --- | --- |
| 5.1 | `GET /cuts/{id}` で 反応・自分の反応・コメントも取る | ⛔ |
| 5.2 | 写真は `img`、動画は `video controls`。`width/height` があれば先に比率を当てる | △ |
| 5.3 | 見出し `YYYY.MM.DD HH:MM`、上に投稿者名 | ✅ |
| 5.4 | ダウンロード（`{url}?download=1` へ遷移） | ⛔ |
| 5.5 | 移動（単体）／削除（確認あり） | ✅ |
| 5.6 | 反応 5種（👍🎉😂🥺🔥）。押すとトグルし、詳細を取り直す | △（送信のみ） |
| 5.7 | **詳しい値の開閉**：撮影日時・UTC・投稿者・種類・タグ・長さ・解像度・カメラ・撮影方法・サイズ・チェックサム・場所・ID | ⛔ |
| 5.8 | **メモ編集**（`PATCH /cuts/{id} {note}` → `メモを保存しました`） | ⛔ |
| 5.9 | コメント一覧＋投稿。自分のコメントには `消す` | △（投稿のみ） |

## 6. その日の詳細（#dayScreen）

> Flutter 版は **web を写さず作り直す**（映像＋縦ドラム）。ただし下記の機能は満たす。

| # | 要件 | 状態 |
| --- | --- | --- |
| 6.1 | 非表示のカットは時間軸に入れない | ⛔ |
| 6.2 | 1カットの長さは `log.cut_seconds`。動画が短ければ実長 | △ |
| 6.3 | 再生／一時停止、前のクリップ、次のクリップ | △（タップで再生停止のみ） |
| 6.4 | 音の出し入れ | ⛔ |
| 6.5 | 全画面 | ⛔ |
| 6.6 | 通しの時計 `M:SS / M:SS` | ⛔ |
| 6.7 | クリップ行：**目のアイコンで非表示切替**（`PATCH /cuts/{id} {hidden}`） | ⛔ |
| 6.8 | クリップ行：**コメント**（件数バッジつき） | ⛔ |
| 6.9 | クリップ行：**詳細** | ⛔ |
| 6.10 | **共有**（共有リンクの作成） | ⛔ |
| 6.11 | **まとめ動画の書き出し**（スタイル設定＋ジョブの進行監視） | ⛔ |
| 6.12 | 全部非表示なら「この日に表示できるクリップがありません」 | ⛔ |

## 7. 撮影（#captureDialog）

| # | 要件 | 状態 |
| --- | --- | --- |
| 7.1 | カメラを開く。失敗したら訳つきトースト、1回だけ再試行 | ✅ |
| 7.2 | **記録先の選択**（`#capDest` → 一覧から選ぶ。既定の記録先は変えない） | ⛔ |
| 7.3 | レンズ選択（2つ以上のときだけ出す。自動→0.5×→1×→望遠→前面の順） | △（前後の切替のみ） |
| 7.4 | ズーム（端末が対応していればレンズ側、しなければ映像の拡大）＋ピンチ | △ |
| 7.5 | 録画秒数は `log.cut_seconds`。輪が一周したら自動で止まる | ✅ |
| 7.6 | 撮影後の確認 → ひとことを添えて保存／撮り直し | ✅ |
| 7.7 | 保存時のメタ：kind・durationMs・takenAt・tzOffset・facing・source・note・lat/lon/accuracy | △（位置情報なし） |
| 7.8 | **位置情報**を撮影時に取得（失敗しても止めない） | ⛔ |
| 7.9 | **端末の動画から取り込む**（＋のチップ） | ⛔ |

## 8. マップ（#mapScreen / #mapListScreen）

| # | 要件 | 状態 |
| --- | --- | --- |
| 8.1 | `GET /cuts?limit=500` のうち位置情報のあるものだけ | ✅ |
| 8.2 | ピンは 44px の丸いサムネ。束ねたものは件数バッジ | ✅ |
| 8.3 | 初回だけ全部が入る位置へ寄せる（余白 40、最大 zoom 16） | ✅ |
| 8.4 | 左下に `N件` | ✅ |
| 8.5 | **押すとその場所のカット一覧へ**（#mapListScreen、新しい順） | ⛔ |
| 8.6 | 位置情報が 0 件なら「位置情報のあるカットがまだありません。」 | ✅ |

## 9. 設定（#settingsScreen）

| # | 要件 | 状態 |
| --- | --- | --- |
| 9.1 | リマインダー：4 つのモード。モードに応じて入力欄を出し分ける | ✅ |
| 9.2 | 保存（`PUT /reminders`） | ✅ |
| 9.3 | **通知の許可**（VAPID 未設定なら案内トースト） | ⛔ |
| 9.4 | テスト送信（送り先が無ければその旨） | △ |
| 9.5 | **アイコン画像の設定／削除** | ⛔ |
| 9.6 | ログアウト（web は確認なし。Flutter は確認あり） | ✅ |
